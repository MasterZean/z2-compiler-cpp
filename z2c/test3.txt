namespace org.z2legacy.ut.overloading

def @main() {
	const C = 7
	C = 8

	test(-1)

	add(1, 5)
	add(1, 5.0)
}

const FF = 20

def test(b: Byte),
def test(b: Word),
def test(b: DWord),
def test(b: QWord),
def test(b: Small),
def test(b: Short),
def test(b: Int),
def test(b: Long),
def test(b: Float),
def test(b: Double),
def test(b: Char),
def test(b: PtrSize) {
	val a = 0
	a = b
	FF = 21
}

func add(a: Int, b: Int): Int,
func add(a: Int, b: Double): Double {
	return a + b
}