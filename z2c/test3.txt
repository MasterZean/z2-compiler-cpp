namespace org.z2legacy.ut.overloading

def @main() {
	test(-1)
}

def test(b: Byte),
def test(b: Word),
def test(b: DWord),
def test(b: QWord),
def test(b: Small),
def test(b: Short),
def test(b: Int),
def test(b: Long),
def test(b: Float),
def test(b: Double),
def test(b: Char),
def test(b: PtrSize) {
	b
}
