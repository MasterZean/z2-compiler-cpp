def @main() {
	const MIN: Small = -128
	test(MIN)
}

def test(b: Byte),
def test(b: Word),
def test(b: DWord),
def test(b: QWord),
def test(b: Small),
def test(b: Short),
def test(b: Int),
def test(b: Long),
def test(b: Float),
def test(b: Double),
def test(b: Char),
def test(b: PtrSize) {
	'a'
	b.class
}

def test(move b: Byte),
def test(move b: Word),
def test(move b: DWord),
def test(move b: QWord),
def test(move b: Small),
def test(move b: Short),
def test(move b: Int),
def test(move b: Long),
def test(move b: Float),
def test(move b: Double), 
def test(move b: Char), 
def test(move b: PtrSize) {
	'm'
	b.class
}


